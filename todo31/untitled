	def get_palindrome(i, string)
		array = []
		array << string[i]
		counter = 1
		if string[i-counter] == string[i+counter]
			array << string[i-counter]
			counter += 1
		end
		array << array[1..-1].reverse
	end

	get_palindrome(3, "racecar")